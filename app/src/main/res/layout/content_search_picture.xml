<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
    android:layout_height="match_parent"

    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_search_picture"
    tools:context=".activities.taskedit.SearchPictureActivity"
    android:id="@+id/content_search_picture"
    android:orientation="vertical"
    android:background="#CEECF5">

    <!-- Notice about how to choose picture -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/myOrange">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="To select the image make a LONG CLICK on it"
            android:textSize="26dp"
            android:padding="6dp"
            android:gravity="center_horizontal"/>
    </LinearLayout>

    <!-- Google search site -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/imageWebSearchPanel">

        <WebView
            android:id="@+id/googleSearchWebView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"></WebView>

    </LinearLayout>
</LinearLayout>